# g8 组审计发现：测试工程（LlamaHarness.Tests 全 34 文件）

> 审计范围：LlamaHarness.Tests 目录 34 文件（含 csproj）；抽样精读 6 个核心测试文件 + 目录全览
> 抽样：PerfAnalyzerTests / GatewayRewriteBaselineTests / TokenGuardTrimTests / SlotAffinityConcurrencyTests / LaunchArgsTests / PrefixFingerprintAndLogFileTests

## 总体评价
测试质量整体高：边界断言密集（阈值相等/严格大于、空值打断、空闲门控、文件锁回归、二分收敛 tokenize 次数上限），
xUnit 异步规范（xUnit1031 WaitAsync 替代阻塞 Wait），跨用例隔离意识强（Cleanup/TestTempPath）。
核心逻辑（PerfAnalyzer/TokenGuard/网关改写/槽位并发/指纹漂移）覆盖扎实。

## 发现清单

- [低] PrefixFingerprintAndLogFileTests.cs:82-108 — Append_FlushesToDiskWithinTimerInterval 测试退化：方法名声称测 LogFile 常驻写入器落盘，实际完全绕过 LogFile
  - 触发条件：P1-H-07 修复（LogFile 单例隔离）后——测试改为自己 new FileStream/StreamWriter 写文件再读回
  - 代码路径：:93-98 自建 FileStream 写入 line → :101-102 读回 Assert.Contains——测的是自己刚写的内容（恒真）；LogFile.Append/LogPipeline 落盘路径零覆盖
  - 破坏后果：LogFile→LogPipeline→磁盘 的 150ms Flush 落盘行为无回归保护；未来异步管道改动（如 Flush 阈值、轮切）不触发测试失败
  - 修复：用 TestTempPath 隔离目录 + 可注入 LogPipeline 实例（构造参数）直接测 Append→等待→文件内容；或删除该测试并新增 LogPipelineTests 内的落盘用例（LogPipelineTests 已覆盖部分）

- [低] LaunchArgsTests.cs:35 — DefaultCfg 硬编码 `KvCachePath = "g:/temp"`，与项目 B-02 治理方向冲突
  - 触发条件：代码维护/拷贝测试基线
  - 代码路径：:35 测试配置硬编码 g:/temp 盘符（B-02 已从产品代码消除该硬编码，测试又引入）
  - 破坏后果：无行为影响（BuildArgs 仅拼接字符串不校验路径），但违背"消除硬编码路径"治理口径，示范性差
  - 修复：改 `KvCachePath = "./kv_cache_test"` 或空串

- [低] SlotAffinityConcurrencyTests.cs:47/:79 — `_cEnteredWaitSignal` 静态字段残留死代码
  - 触发条件：代码维护
  - 代码路径：:47 Interlocked.Exchange(_cEnteredWaitSignal, 1) 写入后无人读取（:79 声明）；实际排队确认靠 cEnteredWait 事件
  - 破坏后果：无行为影响，残留噪音
  - 修复：删除字段与写入行

- [低] TokenGuardTrimTests.cs 全文件 — 行间存在大量空行/稀疏排版（疑似编辑器或代理改写残留）
  - 触发条件：代码阅读/维护
  - 代码路径：全文件语句之间插入大量空行（如 :2-18 连续空行、方法体每行间空行），可读性显著下降
  - 破坏后果：无行为影响；违反项目"注释密度高、代码规范"的既有标准
  - 修复：重排空白（dotnet format 或手排）

- [观察] LaunchArgsTests.cs:151-164 — ExtraArgs 引号剔除行为被测试锁定（含空格路径无法引号包裹的已知权衡）
  - 触发条件：用户配置含空格路径的 ExtraArgs（如 --mmproj "D:\a b\projector.gguf"）
  - 代码路径：:158 构造含引号参数 → :162-163 断言引号被剔除且 EndsWith 拼接结果
  - 破坏后果：行为被锁定为"剔除引号"——含空格路径在 llama-server 侧会被拆分为两个参数导致启动失败（产品注释已声明 B1/B2 权衡，g3 已列 ExtraArgs 白名单条）；测试固化此行为防止回归，属已知设计
  - 建议：文档显式提示用户"含空格路径无法使用"，或在 UI 层做路径校验前置拦截

## 组内统计
- 高：0
- 中：0
- 低：4
- 观察：1

## 重点复查结论
- P1-H-06（Thread.Sleep → ManualResetEvent）：已修复且规范（SlotAffinityConcurrencyTests :38-61/:73-74）
- P1-H-07（LogFile 单例隔离）：已隔离（TestTempPath），但原测试退化为自证（见上低项）
- P1-H-08（slot_bindings.json TOCTOU）：Cleanup 删除共享文件 + 用例内隔离（:16-19），有效
