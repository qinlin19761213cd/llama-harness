LlamaHarness 完整代码评审报告
评审时间：2026-09-04　评审范围：c:\project\lunch\LlamaHarness 全量源码（不含测试快照）　评审方式：4 子代理并行 + 主线程定点验证（Read 确认最高优先级问题行号属实）

一、模块依赖与关键路径
mermaid



HTTP

当前

待迁移

日志

采样

Prometheus

订阅

订阅

主窗口 UI
MainForm.*.cs

SmartSchedulerController

SmartScheduler
+ Gateway / Crash / TokenGuard

IBackendClient
BackendClientFactory

LlamaServerClient

ApiClient 抽象

KvCacheManager
+ RestoreStats / SlotAffinity

OutputContinuer

LlamaFinder

LogLevel / LogCollector
+ RollingLogWriter

KvPersistence.cs

Perf* 系列
Sampler / Analyzer / Overlay / Recorder


100%



问题分布热力：

mermaid



优先修复
局部重要
常规治理
高优先
LogCollector竞态
ValueOf键名未迁移
跨线程Invoke
CTS未Dispose
Font泄漏
HttpResponseMessage泄漏
SaveAsync-slot
Prometheus标签
CLI注入
Backend懒加载
局部 → 全局
低影响 → 高影响
问题严重度 × 影响面

100%



二、模块问题清单（合并去重，共 75 项）
模块 A：调度与 KV 缓存（17 项 · 评级 B）
#	位置	严重度	问题	修复建议
A1	SmartScheduler.cs#L33-47	major	Backend 懒加载无锁 / 无 CAS 双检，并发首次访问会创建多个 LlamaServerClient → HttpClient 泄漏	改为 Lazy<IBackendClient>(factory, LazyThreadSafetyMode.ExecutionAndPublication) 或 Interlocked.CompareExchange 双检
A2	KvCacheManager.cs#L113-124	major	_inflightSaves 键仅含 key，多槽并发保存同 key 会静默写错快照	键改为 (key, slot) 二元组，或引入 ConcurrentDictionary<string, ConcurrentDictionary<int,Task>>
A3	SmartScheduler.cs (多处)	major	HttpResponseMessage / Stream 局部未 using，异常路径泄漏	全部改为 await using var 或显式 try/finally
A4	SmartScheduler.Crash.cs	major	CancellationTokenSource 新建后未 Dispose；异常路径 onTruncation 未真正触发	统一 using；把 truncation 判定接入 CrashDetector.TruncateSlot
A5	RequestProcessor.cs	major	指纹刷新点分散，M-K2 修复后未同步更新 RequestFingerprint	抽出 RefreshFingerprint(slot) 单点函数
A6	SlotAffinity.cs	major	亲和表在并发 slot 释放时可能读到过期条目	加 ReaderWriterLockSlim 或改 ConcurrentDictionary + 版本戳
A7	TokenGuard.cs	minor	Guard 返回 false 时未回滚已消费 token，配额错账	加失败回滚
A8	OutputContinuer.cs	minor	续写循环未对 OperationCanceledException 单独处理，可能被上层误判为业务错误	显式 catch 并向上抛
A9	KvPersistence.cs	minor	快照文件名含未校验字符，与 A8（CLI 注入）联动风险	引入 SafeFileName 工具
A10	LlamaStatsParser.cs	minor	时间戳解析在 InvariantCulture 缺失下会退回机器区域	显式 CultureInfo.InvariantCulture
A11	RestoreStats.cs	minor	恢复后指标未重置 LastSeen，导致 5 分钟内不再采样	恢复即写当前时间戳
A12	SmartScheduler.Gateway.cs	minor	M-K3 onTruncation 挂载位置与实际截断调用点错位	挂载到 CrashDetector.TruncateSlot 单一入口
A13-A17	其余	minor	日志级别、超时参数散落、注释与实现不一致	常量化 + 文档同步
模块 B：HTTP 网关与后端契约（17 项 · 评级 B）
#	位置	严重度	问题	修复建议
B1	LlamaFinder.cs#L71-121	HIGH	--slot-save-path、-m 直接字符串拼接；ExtraArgs 白名单显式允许 "，可注入任意 llama-server flag（后端 host 侧 SSRF / RCE 面）	改用 List<string> argv + ProcessStartInfo.ArgumentList；白名单剔除 "
B2	LlamaFinder.cs#L85	HIGH	cfg.KvCachePath.Trim() 只去首尾空格，中间换行仍可注入 -c 覆盖上下文	用 Path.GetFullPath + 目录白名单校验
B3	SmartScheduler.cs#L33-47	HIGH	同 A1，网关侧并发首次调用放大泄漏风险	见 A1
B4	BackendClientFactory	MAJOR	工厂骨架已建，但 IBackendClient 契约与 LlamaServerClient 未共用 BackendKind 枚举	收敛枚举，统一错误码
B5	LlamaServerClient.cs	MAJOR	无 BaseAddress 校验，可指向任意 host；HttpClient 未共享 → DNS + 连接池重复	引入静态共享 HttpClient；BaseAddress 白名单
B6	LlamaServerClient.cs	MAJOR	SSE 流解析未处理多字节字符跨 chunk 截断	用 UTF8Encoding(false, throwOnInvalidBytes:false) + 缓冲续读
B7	LlamaServerClient.cs	MINOR	超时默认 30s 硬编码	提为配置
B8	网关错误码	MINOR	4xx/5xx 语义混用，无统一 GatewayErrorCode	定义枚举 + JSON 响应体
B9	请求头	INFO	未验证 Content-Type: application/json，接受 multipart 导致解析失败	严格校验
B10	日志	INFO	请求体明文入日志，含敏感 prompt	掩码或仅记长度
B11-B17	其余	MINOR/INFO	心跳、Keep-Alive、并发上限等分散	见各条
模块 C：UI / 日志 / 配置（23 项 · 评级 B）
#	位置	严重度	问题	修复建议
C1	MainForm.*	High	Font 局部变量在循环内 new Font 未 Dispose，长时间运行 GDI 泄漏	改用控件级 Font 或 using
C2	MainForm.*	High	后台任务回调直接访问控件，未 BeginInvoke，跨线程异常	加 IsHandleCreated 判断 + BeginInvoke
C3	RollingLogWriter.cs	High	文件轮转未持有 FileStream 独占，Windows 上切文件抛 IOException	单写者锁 + FileShare.None
C4	LogCollector.cs	High	ConcurrentQueue 消费方未捕获异常，日志丢失	加 try/catch + 死信队列
C5	Config.cs	Medium	数值型配置无上下界校验，-1 可绕过业务保护	加 Math.Clamp
C6-C14	UI 各处	Medium	事件订阅未退订、Timer 未停止、Panel 泄漏	Dispose 覆盖
C15-C23	日志 / 配置	Low	时间戳格式、编码、路径分隔符不一致	常量统一
模块 D：性能监控（18 项 · 评级 C+）
#	位置	严重度	问题	修复建议
D1	PerfSampler.cs#L242-247	major	Prometheus 标签精确匹配 name == "llamacpp:predicted_tokens_seconds"，遇到 {slot="0"} 变体失败 → 吞吐指标长期 null	用 line.IndexOf('}') 剥离标签后再比
D2	PerfAnalyzer.cs (ValueOf)	major	键名从 M-K2 之前版本未迁移，tokens_per_second 仍读旧名 → 面板恒 0	加别名或统一命名
D3	PerfSampler.cs	major	DateTime.Now 非单调时钟，回拨时采样间隔变负 → 除零	改 Stopwatch.GetTimestamp() 或 Environment.TickCount64
D4	PerfRecorder.cs	major	采样线程未取消，Application.Exit 后仍写文件	引入 CancellationToken
D5	PerfOverlay.cs	major	覆盖层绘制未节流，60fps 下 CPU 飙	Timer 节流到 200ms
D6	PerfAnalyzer.cs	major	窗口化聚合在数据稀疏时统计偏差	加样本数下限
D7	PerfOverlay.cs	major	GDI+ 对象未 Dispose（同 C1）	集中释放
D8	PerfSampler.cs	major	HttpClient 每次采样新建 → DNS 抖动	复用 StaticClient
D9-D18	其余	minor	测试覆盖、边界值、单位换算	补测试
监控盲区：PerfSampler / PerfAnalyzer 均无单元测试；Prometheus 解析、时区、非单调时钟三条关键路径完全裸奔，D1/D2/D3 三项可解释生产上"吞吐恒为 0"的现象。

三、整体健康度评级
模块	评级	依据
调度 + KV 缓存	B	主线逻辑正确，但并发/资源管理有 6 个 major，A1/A2 可直接引发生产事故
HTTP 网关 + 后端契约	B	骨架合理，B1（CLI 注入）为高危，B5 需长期治理
UI + 日志 + 配置	B	功能可用，C1-C4 属于运维级隐患
性能监控	C+	D1+D2 联合让核心指标长期失效，D3 时钟问题在回拨场景必现
项目整体	B	可交付但需先修 Top3，性能监控模块应作为下一迭代 P0
四、Top 3 项目级优先修复
SmartScheduler.Backend 懒加载线程安全（A1 / B3 · HIGH）
网关冷启动并发请求直接造成 HttpClient 泄漏；修复代价低（10 行内），必须最先修。

LlamaFinder.BuildArgs CLI 注入（B1 / B2 · HIGH）
--slot-save-path + ExtraArgs 双引号白名单构成后端 SSRF / flag 注入面；生产部署前必修。

PerfSampler 指标解析 + PerfAnalyzer 键名对齐（D1 / D2 · major）
两个 bug 联合导致吞吐指标"沉默失效"，用户无法发现监控失效；修复后 D3 一并落地。

五、建议修复批次
批次	内容	优先级
P0	A1, B1, B2, D1, D2, D3	阻塞发布
P1	A2, A3, A4, B5, C1, C3, C4, D4, D5	发布前完成
P2	其余 major	下一迭代
P3	全部 minor / info	常态治理